Git is a software where as git hub is a tool to host git

they are tons of other stuff which are available on the market like github

git helps us to track the software like games

going back and fourth

version control is a thing which is used to to do that
which is tracking

TERMINOLOGY OF GIT 
Repository = folder

ls -la to see hidden folders like git

to to the folder in the terminal
and type git init

this will create a hidden folder which will track everything

The joutney of git 

git init->choose a working directory->git add(some another commands also available here)
->Staging area->commit->it reflect in repo(folder)->push->Saving in git hub

no to add file in the staging area git add filename else
git add .
git restore --staged filename(To unstage files)

git commit -m "mesage"
to commit


you have to do this every time you make certain changes


to know what you have did so far

git log 

u can use this with certain flags also like --oneline

lets talk about gitIgnore
it tell the computer which files and directory to be ignored
This file usually have some sensitive info
for example now i want the four named file to be ignored


Git also ignores empty folder

gitkeep
we want some empty files to stored by git
so we include .gitkeep files inside them like what 
i have did here i  five Repository